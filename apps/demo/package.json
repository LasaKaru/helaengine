{
  "name": "@helaengine/demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal, framework-free harness that renders a scene document with the HelaEngine runtime.",
  "scripts": {
    "dev": "vite --port 5173 --strictPort",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@helaengine/engine": "workspace:*",
    "@helaengine/schema": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
