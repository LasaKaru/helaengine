{
  "name": "@helaengine/realtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Colyseus co-op server. A separate deployable service from the API: a different workload shape.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@colyseus/schema": "2.0.36",
    "@helaengine/engine": "workspace:*",
    "@helaengine/schema": "workspace:*",
    "colyseus": "0.15.17",
    "three": "^0.172.0"
  },
  "devDependencies": {
    "@types/three": "^0.172.0",
    "colyseus.js": "^0.15.26",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
