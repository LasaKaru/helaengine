{
  "name": "@helaengine/share",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hosts validated builds behind a link. A separate deployable service from the API and the co-op server.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@helaengine/schema": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.23.5",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
