/**
 * Engine version, pinned into every export so an old bundle never silently picks up new runtime
 * behaviour. Keep in sync with `package.json` when releasing.
 */
export const ENGINE_VERSION = '0.1.0';
