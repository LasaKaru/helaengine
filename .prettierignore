node_modules
dist
.turbo
coverage
pnpm-lock.yaml

generated
raw-assets

# Generated by `pnpm gen-docs` from the engine's schemas. Prettier reformatting it would put the
# committed file permanently at odds with what the generator emits, and `gen-docs --check` would
# fail every run.
docs/BEHAVIOURS.md
